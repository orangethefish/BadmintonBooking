image: mcr.microsoft.com/dotnet/sdk:8.0

clone:
  depth: full

pipelines:
  default:
    - parallel:
        - step:
            name: Build and Test
            caches:
              - dotnetcore
            script:
              - REPORTS_PATH=./test-reports/build_${BITBUCKET_BUILD_NUMBER}
              - dotnet restore
              - dotnet build --no-restore --configuration Release
              - dotnet test --no-build --configuration Release --test-adapter-path:. --logger:"junit;LogFilePath=$REPORTS_PATH/junit.xml"
            artifacts:
              - test-reports/**
        - step:
            name: Lint the code
            caches:
              - dotnetcore
            script:
              - export SOLUTION_NAME=BadmintonBooking
              - export REPORTS_PATH=linter-reports
              - dotnet new tool-manifest
              - dotnet tool install JetBrains.ReSharper.GlobalTools
              - dotnet tool restore
              - dotnet jb inspectcode ${SOLUTION_NAME}.sln --output="${REPORTS_PATH}/jb-${BITBUCKET_BUILD_NUMBER}.xml"
            artifacts:
              - linter-reports/**
    
    - step:
        name: Build and Push Docker Image
        image: docker:24.0.7
        services:
          - docker
        script:
          - echo "$DOCKER_HUB_PASSWORD" | docker login -u "$DOCKER_HUB_USER" --password-stdin
          - export IMAGE_NAME=${DOCKER_HUB_USER}/badmintonbooking:${BITBUCKET_BUILD_NUMBER}
          - docker build -t $IMAGE_NAME .
          - docker push $IMAGE_NAME

    - step:
        name: Mirror to GitHub
        image: atlassian/default-image:4
        script:
          - git fetch --all
          - git fetch --tags
          - git push --mirror git@github.com:orangethefish/BadmintonBooking.git
